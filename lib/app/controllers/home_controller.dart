import 'package:get/get.dart';
import 'package:injectable/injectable.dart';

@lazySingleton
class HomeController extends GetxController {
  // Placeholder — expand when building the real home screen
}
